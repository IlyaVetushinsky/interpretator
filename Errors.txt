1) создание переменных с повторяющимся именем
2) чтобы метка не заходила в тело функции извне и не выходила вовне
3) операции с не инициализированными  переменными
	if (VarStore.find(k) != VarStore.end() && VarStore[k].find(a1) != VarStore[k].end()) {
		c = VarStore[k][a1];
	}
4)