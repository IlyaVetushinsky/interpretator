1)+++ создание переменных с повторяющимся именем другого типа (номера целочисленной и логической переменной, массивов и процедур в пределах одной
программы совпадать не могут)
2)+++ чтобы метка не заходила в тело функции извне и не выходила вовне
3)+++ операции с не инициализированными  переменными
	if (VarStore.find(k) != VarStore.end() && VarStore[k].find(a1) != VarStore[k].end()) {
		c = VarStore[k][a1];
	}
4)+++ если к константе применяются действия котрые должны быть для переменной
5)+++ присвоение переменной одного типа значение другого(проверка для процедур <-)
5.1)+++ вывод переменной процедуры
6)+++ неправильная индексация